import { createMuiTheme, Theme } from '@material-ui/core/styles';

export const theme: Theme = createMuiTheme({
  palette: {
    primary: {
      contrastText: "#fff",
      main: "#3F6C96",
    },
    secondary: {
      contrastText: "#fff",
      main: "#324F4E",    
    },
  },
});
